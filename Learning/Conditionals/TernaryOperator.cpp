#include<iostream>
using namespace std;

int main()
{

    int a = 50;
    int b = 10;

    a > b ? cout << "A is greater" : cout << "B is greater";

    return 0;
}

/*
 *
 * In-notes
 * --------
 * <statment> ? <if true this code will work> : <if false than this will work>
 *
 * the question mark is asking "is this true chat?", if yes then content ahead of it will work,
 * or the colun will work like a else, if not that then this.
 *
 * Pretty short and easy chapter yet really important.
 *
 * Congrats this the end. (of this chapter)
 *
 * Next: Loops > "ForLoops"; for loops for loop four loop.
 * Practice program: "Calculator";
 *
 */